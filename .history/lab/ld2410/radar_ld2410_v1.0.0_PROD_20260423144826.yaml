# =================================================================
# 🛠️ PACKAGE : CONFIGURATION RADAR LD2410
# Fonction : Helpers pour le pilotage et l'affichage du Dashboard
# =================================================================

input_select:
  liste_esp_ld2410:
    name: "Choisir un radar LD2410"
    icon: mdi:radar
    options:
      - esp_multi_capteur
      - esp_salon
      - esp_sdb
      - esp_chambre
      - esp_cuisine
    initial: esp_sdb

input_boolean:
  radar_affichage_sliders:
    name: "Affichage des curseurs (Radar)"
    icon: mdi:tune
    initial: off
